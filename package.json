
{
  "name": "task-manager-api",
  "version": "1.0.0",
  "description": "API REST de gestion de tâches — Projet CI/CD avec PM2 & GitHub Actions",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "pm2 start ecosystem.config.js",
    "prod": "pm2 start ecosystem.config.js --env production",
    "staging": "pm2 start ecosystem.config.js --env staging",
    "stop": "pm2 stop task-api",
    "restart": "pm2 restart task-api",
    "reload": "pm2 reload task-api",
    "logs": "pm2 logs task-api",
    "monitor": "pm2 monit",
    "cleanup": "pm2 delete task-api",
    "test": "jest --verbose --forceExit --detectOpenHandles",
    "test:watch": "jest --watch --verbose"
  },
  "keywords": ["express", "pm2", "cicd", "api", "rest"],
  "author": "Votre nom",
  "license": "MIT",
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "express": "^4.x.x"
  },
  "devDependencies": {
    "jest": "^29.x.x",
    "supertest": "^6.x.x"
  }
}